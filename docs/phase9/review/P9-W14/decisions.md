# P9-W14 Decisions

1. Phase 9 is marked complete only after fresh reruns proved all aggregate gates pass on current branch state.
2. Table writer compatibility metadata was corrected in Phase 9 rather than deferred because it directly blocked required mini→casacore matrix cells.
3. W13/W14 are now explicit gated waves in tooling (`check_phase9_w13.sh`, `check_phase9_w14.sh`) to keep plan and gates aligned.
