# P12-W3 Decisions

1. DELETE removes rows from back to front to keep indices stable during iteration.
2. INSERT SELECT is deferred to W5 (subquery support).
3. Column count mismatch in INSERT throws a clear error.
